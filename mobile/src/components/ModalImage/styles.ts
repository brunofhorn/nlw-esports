import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scroll: {
    marginVertical: 20,
  },
  image: {
    width: 300,
    maxHeight: 400,
    height: 400,
    borderRadius: 6,
  },
  title: {
    fontSize: 30,
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 20,
  },
});
